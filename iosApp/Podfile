target 'iosApp' do
  use_frameworks!
  platform :ios, '16.0'
  pod 'shared', :path => '../shared'
  # Native Firebase SDK that GitLive's firebase-auth klib links against.
  # Keep the major version aligned with the GitLive firebase-auth release.
  pod 'FirebaseAuth'
end